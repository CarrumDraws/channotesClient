import React, { useState } from "react";
import { useSelector } from "react-redux";
import { Box, Typography, useTheme } from "@mui/material";

function NavbarBuffer() {
  return <Box height="5rem" width="100%"></Box>;
}

export default NavbarBuffer;
