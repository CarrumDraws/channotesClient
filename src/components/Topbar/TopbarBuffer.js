import React, { useState } from "react";
import { Box } from "@mui/material";

// Buffer to push page elements away from TopbarBuffer
function TopbarBuffer() {
  return <Box height="5rem" width="100%" />;
}

export default TopbarBuffer;
